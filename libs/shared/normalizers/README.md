# normalizers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build normalizers` to build the library.

## Running unit tests

Run `nx test normalizers` to execute the unit tests via [Jest](https://jestjs.io).
